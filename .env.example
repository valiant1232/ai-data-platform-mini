# -----------------------------
# Database (Postgres)
# -----------------------------
POSTGRES_DB=aiplatform
POSTGRES_USER=aiplatform
POSTGRES_PASSWORD=change_me_strong_password

# -----------------------------
# Auth (JWT for this platform)
# -----------------------------
JWT_SECRET=change_me_super_secret
JWT_EXPIRE_MINUTES=120

# -----------------------------
# Label Studio (External annotation UI)
# -----------------------------
# Base URL of your Label Studio instance (cloud or self-hosted)
LS_BASE_URL=https://lancetops.com

# IMPORTANT:
# Use Label Studio API token / JWT refresh token here (DO NOT commit real token).
# For cloud: usually you can obtain it via API or account settings.
LS_API_TOKEN=REPLACE_WITH_YOUR_LABEL_STUDIO_REFRESH_TOKEN

# Label Studio project id to sync tasks/predictions with
LS_PROJECT_ID=1

# -----------------------------
# Ollama (Local inference)
# -----------------------------
# For Docker Desktop on macOS/Windows, use host.docker.internal to reach host machine.
# If Ollama runs inside another container/network, point to that reachable URL.
OLLAMA_BASE_URL=http://host.docker.internal:11434

# Model name that exists in `ollama list` (e.g. llama3.2:1b)
OLLAMA_MODEL=llama3.2:1b

# Request timeout in seconds
OLLAMA_TIMEOUT=120
